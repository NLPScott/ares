分布式搜索架构
1、整个集群对外部呈去中心化，集群内部采用Zookeeper进行协调
2、可以存储多个索引库，多个索引库之间互不影响，
3、每个索引库的索引可根据内存使用量自动sharding,每个shard可以指定副本数
4、客户端写入索引时不必等待所有的所有副本同步完成，只要一个写入成功即可返回，但是每个replicas任一时刻只有一个节点可写入且这个节点原本须是存有最新数据的节点
5、客户端请求首先根据索引库路由请求到对应的索引库上，然后获取存有最新数据的shard集合，然后根据集群负载路由    请求
6、采用HBase进行存储