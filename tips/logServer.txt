日志系统
日志系统采用client-server模式

server集群负责日志记录，但是各个server间互不通信，每个server上都可以配置多个端口接受client端的请求，server是分组的

client端负责负载均衡、failover，以守护进程的方式部署在各个应用服务器主机上，可以配置多个端口以接受应用服务器的写入请求，同时
为各种语言的连接client socket端口都写了相应的插件调用程序，由插件负责连接哪个端口